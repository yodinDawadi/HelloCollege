PORT=4000
JWT_SECRET=replace-with-a-long-random-secret
MONGODB_URI=mongodb://127.0.0.1:27017/campus_complaints
# Set true only when MongoDB is available; false uses the included in-memory repository for demos/tests.
USE_MONGODB=false
UPLOAD_DIR=uploads
MAX_FILE_SIZE_MB=5
